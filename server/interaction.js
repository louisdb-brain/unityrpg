export class interaction{
    constructor(pSkill,pItemdrops){
        this.io=io;
        this.skill=pSkill;
        this.items=pItemdrops;
    }
    consumeItem(playerid,inventory){
        for (let i = 0; i < inventory.length; i++){
            inventory.length.remov
        }

    }
    getItem(playerid,inventory){
        if(inventory.length < inventory.maxLength){}
    }
    skillcheck(){}
    giveExp(){}
}